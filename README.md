This project uses python program and codes n decodes messages in  secret language
code rules:
1) if the word is or longer than 3, take the 1st letter and appends it to the end n add 3 random alphabets to the start n end of the word
2) if the word is less than 3 letters it reverses it.
decode rules:
1) reverses 2 or less letter words
2) removes the extra letters n put tge 1st letter back to its place
